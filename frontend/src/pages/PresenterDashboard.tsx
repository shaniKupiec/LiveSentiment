import React from "react";
import { Typography, Box } from "@mui/material";

const PresenterDashboard: React.FC = () => (
  <Box p={4}>
    <Typography variant="h3">Presenter Dashboard</Typography>
    <Typography>Placeholder for managing presentations, polls, and viewing sentiment.</Typography>
  </Box>
);

export default PresenterDashboard; 