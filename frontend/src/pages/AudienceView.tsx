import React from "react";
import { Typography, Box } from "@mui/material";

const AudienceView: React.FC = () => (
  <Box p={4}>
    <Typography variant="h3">Audience View</Typography>
    <Typography>Placeholder for joining presentations and submitting responses.</Typography>
  </Box>
);

export default AudienceView; 