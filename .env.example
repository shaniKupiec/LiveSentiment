# ===========================================
# LiveSentiment Environment Configuration
# ===========================================
# Copy this file to .env and fill in your actual values
# Never commit the .env file to version control

# ===========================================
# API Keys for Development
# ===========================================

# Groq API Key - Required for NLP analysis
# Get your API key from: https://console.groq.com/
# 1. Sign up or log in to Groq Console
# 2. Navigate to API Keys section
# 3. Create a new API key
# 4. Copy the key and replace the value below
GROQ_API_KEY=your-actual-groq-api-key-here

# ===========================================
# Database Configuration (for local development)
# ===========================================

# Database host - Use localhost for local development
DB_HOST=localhost

# Database port - Default PostgreSQL port
DB_PORT=5432

# Database name - Name of your database
DB_NAME=livesentiment

# Database username - PostgreSQL username
DB_USER=postgres

# Database password - PostgreSQL password
DB_PASSWORD=postgres

# ===========================================
# JWT Configuration (for local development)
# ===========================================

# JWT Secret Key - Generate a secure random key (minimum 32 characters)
# You can generate one using: openssl rand -base64 32
# Or use an online generator: https://generate-secret.vercel.app/32
JWT_KEY=your-super-secret-key-with-at-least-32-characters-for-jwt-signing

# JWT Issuer - Application identifier
JWT_ISSUER=LiveSentiment

# JWT Audience - Token audience
JWT_AUDIENCE=LiveSentiment

# ===========================================
# Frontend Configuration
# ===========================================

# API Base URL - Backend API URL for frontend
# Use localhost:5000 for local development
VITE_API_URL=http://localhost:5000

# ===========================================
# Production Environment Variables
# ===========================================
# The following variables are used in production deployment
# You don't need to set these for local development

# ASP.NET Core Environment
# ASPNETCORE_ENVIRONMENT=Production

# ASP.NET Core URLs (for production)
# ASPNETCORE_URLS=http://0.0.0.0:10000

# Database SSL Mode (for production)
# DB_SSL_MODE=Require

# ===========================================
# Security Notes
# ===========================================
# 1. Never commit your .env file to version control
# 2. Use different API keys for development and production
# 3. Rotate your API keys regularly
# 4. Use strong, unique JWT keys
# 5. Keep your database credentials secure

# ===========================================
# Getting Started
# ===========================================
# 1. Copy this file: cp env.example .env
# 2. Get your Groq API key from https://console.groq.com/
# 3. Generate a secure JWT key (32+ characters)
# 4. Update the values above with your actual keys
# 5. Start the development environment: docker-compose -f docker-compose.dev.yml up --build
